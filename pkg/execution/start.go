package execution

import "sinhasoftware.solutions/kalakaar/pkg/configuration"

const defaultParallelism = 20

func Start(p *configuration.Pipeline) error {
	// todo: based on the license we can decide what is supported here.
	// this should be pulled up in some validator too

	//setup := p.Setup
	// todo: based on the plan and other settings determine the parallelism

	// todo: Create

	return nil
}
